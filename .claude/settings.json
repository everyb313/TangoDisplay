{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --name-only -- '*.swift' | grep -q . && swift build 2>&1 | tail -20"
          }
        ]
      }
    ]
  }
}
